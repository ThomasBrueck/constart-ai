export interface UserInput {
    email: string;
    username: string;
    password: string;
    confirmPassword: string;
}

export interface UserInputLogin {
    email: string;
    password: string;
}